// Base44 doesn't require a special _app.js file
// This is a placeholder file to maintain project structure
// The actual routing is handled by the layout.jsx file

export default function App({ Component, pageProps }) {
  return <Component {...pageProps} />;
}